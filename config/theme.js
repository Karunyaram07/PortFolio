/**
 * Layout and structural design tokens.
 */
export const layout = {
  maxWidth: "max-w-7xl",
  maxReadingWidth: "max-w-3xl",
  sectionPadding: "py-20 md:py-32",
  containerPadding: "px-6 md:px-12",
}

/**
 * Proportional Border Radius classes.
 */
export const borderRadius = {
  sm: "rounded-sm",
  md: "rounded-md",
  lg: "rounded-lg",
  xl: "rounded-xl",
  "2xl": "rounded-2xl",
  "3xl": "rounded-3xl",
  "4xl": "rounded-4xl",
  full: "rounded-full",
}

/**
 * Standardized soft shadow presets (extremely subtle, Linear/Vercel style).
 */
export const shadows = {
  sm: "shadow-xs",
  md: "shadow-sm",
  lg: "shadow-md",
  xl: "shadow-lg",
  none: "shadow-none",
}

/**
 * Common shared design patterns and classes.
 */
export const classes = {
  glass: "glass",
  glassStrong: "glass-strong",
  glassHover: "glass-hover",
  interactive: "transition-all duration-300 hover:scale-[1.02] active:scale-[0.98]",
  gradientText: "bg-clip-text text-transparent bg-gradient-to-r from-gradient-start to-gradient-end",
  focusRing: "focus-visible:ring-3 focus-visible:ring-ring/50 focus-visible:outline-none",
  scrollbar: "scrollbar-thin scrollbar-thumb-muted/30 scrollbar-track-transparent",
}
