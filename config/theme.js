/**
 * Layout and structural design tokens.
 */
export const layout = {
  maxWidth: "max-w-7xl",
  sectionPadding: "py-20 md:py-32",
  containerPadding: "px-6 md:px-12",
}

/**
 * Common shared design patterns/classes.
 */
export const classes = {
  glass: "backdrop-blur-md bg-white/70 dark:bg-black/70 border border-black/10 dark:border-white/10",
  interactive: "transition-all duration-300 hover:scale-[1.02] active:scale-[0.98]",
  gradientText: "bg-clip-text text-transparent bg-gradient-to-r from-neutral-900 to-neutral-500 dark:from-white dark:to-neutral-400",
}
